import React from 'react';
import {
    HeaderWrapper,
    Title
} from './styles';

const Header = () => {
    return (
        <HeaderWrapper>
            <Title>Lista de Tareas</Title>
        </HeaderWrapper>
    );
};

export default Header;
