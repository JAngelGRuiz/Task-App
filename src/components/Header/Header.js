import React from 'react';
import {
    HeaderWrapper,
    Title
} from './styles';

const Header = () => {
    return (
        <HeaderWrapper>
            <Title>To Do App</Title>
        </HeaderWrapper>
    );
};

export default Header;
